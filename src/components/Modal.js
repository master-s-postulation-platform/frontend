/* import React, { Component } from 'react';
import Portal from './Portal';

export default class Modal extends Component {
  render() {

    const { children, toggle, action } = this.props;

    return (
       <Portal>
        {active && {
          <div>n</div>
        }}
       </Portal>
    );
  }
} */